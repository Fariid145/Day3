We declare an array by defining the variable type  and a square bracket in front;
we reference an element in an array by refering to the index number of the element;

The memory is allocated when an array is created.